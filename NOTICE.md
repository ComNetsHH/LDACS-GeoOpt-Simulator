# NOTICE

This repository is a paper-specific simulator and is intended to host:
- OMNeT++ scenario files
- execution scripts for local and HPC runs
- result extraction and plotting scripts

The core GeoOpt routing implementation is expected to live in a separate routing
repository and is referenced from this simulator through workspace-relative paths.
